import { css } from '@emotion/react/macro';

const styles = {
  nameEditorView: css({
    color: 'hotpink',
    display: 'flex',
    alignItems: 'center',
    flexDirection: 'column',
  }),
};

export default styles;
