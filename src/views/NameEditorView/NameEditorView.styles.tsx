import { css } from '@emotion/react';

const styles = {
  nameEditorView: css({
    color: 'hotpink',
    display: 'flex',
    alignItems: 'center',
    flexDirection: 'column',
  }),
};

export default styles;
