const theme = {
  borderRadius: 4,

  colors: {
    background: '#fefefe',
    border: '#8b8b8b',
    chat: '#01f101',
    white: '#fff',
  },

  spacing: (factor: number) => 3 * factor,
};

export default theme;
